version: "v1.0"
name: "z4pi"

agent:
  machine:
    type: "e1-standard-2"
    os_image: "ubuntu2004"

blocks:
  - name: "Build project"
    task:
      jobs:
        - name: Setup
          commands:
            - checkout
            - sem-version go 1.17.1
            - make build
        - name: Test
          commands:
            - checkout
            - sem-version go 1.17.1
            - make test
